<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asus_desktop.remask">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splashscreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login"></activity>
        <activity android:name=".Register"></activity>
        <activity android:name=".MainActivityBackup"></activity>
        <activity android:name=".BuatCatatan"></activity>
        <activity android:name=".RegisterNext"></activity>
        <activity android:name=".BantuanActivity"></activity>
        <activity android:name=".PopActivity"
            android:theme="@style/AppTheme.PopMe">
        </activity>
        <activity android:name=".Grafik.Grafik"></activity>
        <activity android:name=".JoinGroup"></activity>
        <activity android:name=".Buat_Catatan_Pendidikan"></activity>
        <activity android:name=".BuatCatatanLain"></activity>
        <activity android:name=".CustomDialog"
            android:theme="@style/AppTheme.CustomDialog">
        </activity>
        <activity android:name=".Progress.MainActivityProgress"></activity>
        <receiver android:name=".Alarm.AlarmReceiver"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>